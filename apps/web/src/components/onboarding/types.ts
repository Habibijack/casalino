export interface CompanyProfileData {
  name: string;
  contactEmail: string;
  phone: string;
  address: string;
  postalCode: string;
  city: string;
  canton: string;
}

export interface TeamInvite {
  email: string;
  role: string;
}
